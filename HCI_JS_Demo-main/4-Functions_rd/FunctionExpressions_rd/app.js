// function add(x, y) {
//     return x + y;
// }

// defines a simple function using a function expression in JavaScript. This function, named add, takes two parameters, x and y, and returns their sum.
const add = function (x, y) {
  return x + y;
};
